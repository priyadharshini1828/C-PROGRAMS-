#include<stdio.h>
int main()
{
    int n,i,j;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    if(a[i]<n+1)
    {
        printf("deletion is not possible");
    }
    else
    {
        printf("deletion is possible");
    }
     return 0;
}
